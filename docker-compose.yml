services:
  web:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      PORT: "3000"
      HOST: "0.0.0.0"
      DATA_DIR: /app/data/store
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://veltrano.ma}
      SITE_URL: ${SITE_URL:-https://veltrano.ma}
      CORS_ORIGINS: ${CORS_ORIGINS:-https://veltrano.ma,https://www.veltrano.ma}
      DATABASE_URL: ${DATABASE_URL:-}
      ADMIN_SECRET: ${ADMIN_SECRET:-}
      WHATSAPP_TOKEN: ${WHATSAPP_TOKEN:-}
      WHATSAPP_PHONE_NUMBER_ID: ${WHATSAPP_PHONE_NUMBER_ID:-}
      WHATSAPP_GRAPH_VERSION: ${WHATSAPP_GRAPH_VERSION:-v22.0}
      TWILIO_ACCOUNT_SID: ${TWILIO_ACCOUNT_SID:-}
      TWILIO_AUTH_TOKEN: ${TWILIO_AUTH_TOKEN:-}
      TWILIO_WHATSAPP_FROM: ${TWILIO_WHATSAPP_FROM:-}
    volumes:
      - veltrano-orders:/app/data/store
      # Optional: catalogue photos (~720MB), not in git
      # - ./public/products:/app/public/products
    restart: unless-stopped

volumes:
  veltrano-orders:
